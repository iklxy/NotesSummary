# Work Buddy 项目交接文档

## 1. 项目概述

Work Buddy 是一个面向访谈音频处理的端到端系统，当前代码由三部分组成：

1. Engine：根目录 Python 服务，负责音频上传、ASR、纠错清洗、summary 落库、RAG、Notes/Minutes/KBQ/Cards/CA 生成。
2. BFF：`summarynotes-be/` 下的 FastAPI 服务，负责对前端提供业务 API、文件上传、权限和导出。
3. FE：`summarynotes-fe/` 下的 Next.js 前端，负责项目、问卷、访谈、纪要、卡片、CA 的页面展示和编辑。

核心业务目标是把一段访谈音频逐步处理成：

- 可编辑的转录全文 trans
- 一份访谈级智能纪要 Minutes
- 基于 key BQ 的 KBQ Notes
- 全文模块总结 Cards
- 项目级跨访谈 CA 对比表

## 2. 系统架构与技术栈

### 2.1 架构分层

```text
Next.js FE(3000)
    |
    v
FastAPI BFF(9000)
    |
    +--> MySQL
    +--> 本地 data/audio 文件
    +--> 内部调用 Engine(8000)
    |
    v
FastAPI Engine(8000)
    |
    +--> TOS 音频上传/预签名
    +--> 火山 ASR
    +--> LLM(OpenAI 兼容 / Anthropic 兼容)
    +--> Ollama Embedding
    +--> Qdrant 向量检索
```

### 2.2 技术栈

- 后端语言：Python
- Web 框架：FastAPI、Uvicorn
- 前端：Next.js 16、React 19、TypeScript、Ant Design、React Query、Tailwind 4
- 数据库：MySQL
- 向量库：Qdrant
- Embedding：Ollama
- ASR：火山/豆包录音识别
- 对象存储：字节 TOS
- LLM：OpenAI 兼容接口、Anthropic 兼容接口
- 依赖管理：Conda + npm

## 3. 启动方式、配置文件与依赖

### 3.1 基础依赖

启动前需要先准备以下外部依赖：

- MySQL
- Qdrant
- Ollama，并拉起 embedding 模型
- 可访问的 LLM API
- 火山 ASR 账号与密钥
- TOS 账号、Bucket 与密钥
- Node.js/npm
- Conda

### 3.2 配置文件

#### 根目录 `.env`

Engine 和 BFF 都依赖根目录 `.env`。配置读取入口在 [config.py]

核心配置项：

- 数据库：`DB_HOST` `DB_PORT` `DB_USER` `DB_PASSWORD` `DB_NAME`  数据库默认使用MySQL
- DB_HOST 数据库主机IP
- DB_PORT 数据库端口
- USER 用户名
- PASSWORD 密码
- NAME 数据库名
  
- 通用 LLM：`LLM_PROVIDER` `LLM_API_KEY` `LLM_BASE_URL` `LLM_MODEL_NAME` 
- 已经废弃，无需配置
  
- 转录纠错链路 LLM：`TRANSCRIPT_LLM_PROVIDER` `TRANSCRIPT_LLM_API_KEY` `TRANSCRIPT_LLM_BASE_URL` `TRANSCRIPT_LLM_MODEL_NAME`
- 对trans进行纠错、再纠错、清洗的模型配置，默认使用DeepSeek v4 pro
- PROVIDER 保持填写openai不要改动 或者可以改为anthropic，DeepSeek v4 pro兼容openai和anthropic两个接口
- API_KEY 填写所用模型的key即可
- BASE_URL 根据Provider是openai or anthropic动态更改，官方接口文档中有
- MODEL_NAME 根据官方接口文档填写即可，建议不改动，当前使用的是flash，可以改动为pro，但可能思考时间过长导致整体时间链路拉长
  
- Notes/Minutes/KBQ/CA LLM：`NOTES_LLM_PROVIDER` `NOTES_LLM_API_KEY` `NOTES_LLM_BASE_URL` `NOTES_LLM_MODEL_NAME`
- 生成Key BQ Notes、智能纪要、CA表格的模型，使用的是火山引擎上的豆包seed2.0pro
- Provider 兼容接口，建议保持填写openai，如果需要改动兼容接口需要先查阅官方文档
- API_KEY 正常填写key
- BASE_URL openai兼容接口的URL
- MODEL_NAME 查阅官方文档进行填写
- 
- Ollama：`OLLAMA_HOST` `OLLAMA_MODEL_NAME`
- embedding model相关配置
- HOST要完整填写为 类似 http://124.221.2x.111:11434，即需要加上端口，而不是简单填写IP
- MODEL_NAME embedding model的名称 如 bge-m3:latest
- 需要在本地部署ollama之后安装bge-m3:latest，详见部署文档

- ASR：`ASR_APP_KEY` `ASR_ACCESS_KEY` `VOLCANO_CLUSTER` `VOLCANO_SERVICE_URL`
- TOS：`TOS_ACCESS_KEY` `TOS_SECRET_KEY` `TOS_ENDPOINT` `TOS_REGION` `TOS_BUCKET_NAME` `LOCAL_AUDIO_ROOT` `TOS_AUDIO_PREFIX` `TOS_URL_EXPIRE_SECONDS`
- 以上两个参数不要进行任何改动
  
- Qdrant：`QDRANT_HOST` `QDRANT_PORT` `QDRANT_COLLECTION_SUMMARY`
- 向量数据库 部署详见部署文档
  
- BFF 调 Engine：`INTERNAL_SERVICE_BASE`
- 热词兜底：`TERM_HINTS_FILE`
- 已被废弃，保持原状即可

#### Python 依赖

- Conda 环境文件：[environment.yml]

#### 前端依赖

- 前端依赖定义：[summarynotes-fe/package.json]
- 前端默认请求 BFF 地址在 [summarynotes-fe/lib/apiClient.ts]

### 3.3 启动命令

#### 推荐方式：统一脚本

统一启动脚本为 [start_all.sh]

```bash
conda env create -f environment.yml
conda activate vol

cd /home/lixinyang/NotesSummary/summarynotes-fe
npm install

cd /home/lixinyang/NotesSummary
bash start_all.sh start
```

脚本支持：

```bash
bash start_all.sh start
bash start_all.sh stop
bash start_all.sh restart
bash start_all.sh status
```

默认端口：

- Engine：`8000`
- BFF：`9000`
- FE：`3000`

## 此处如果 8000 9000 3000端口已被占用，直接修改启动start_all.sh中的启动脚本即可，目标代码块是
```
ENGINE_PORT="${ENGINE_PORT:-8000}"
BFF_PORT="${BFF_PORT:-9000}"
FE_PORT="${FE_PORT:-3000}"
```

## 前端修改启动端口之后，后端的中间件鉴权需要同步放行，位置在 summarynotes-be/middleware/cors.py，需要把新端口在setup_cors函数的origins进行注册

补充说明：

- `start_all.sh` 会自动创建 `runtime/logs`、`runtime/pids`
- 前端如果没有构建产物，会自动执行 `npm run build`
- `NEXT_PUBLIC_API_BASE_URL` 未设置时，脚本会自动拼接为当前机器 IP + `9000`

#### 手动启动

Engine：

```bash
cd /home/lixinyang/NotesSummary
uvicorn Api:app --host 0.0.0.0 --port 8000
```

BFF：

```bash
cd /home/lixinyang/NotesSummary/summarynotes-be
uvicorn main:app --host 0.0.0.0 --port 9000
```

FE：

```bash
cd /home/lixinyang/NotesSummary/summarynotes-fe
npm install
npm run dev
```

### 3.4 产品依赖

从业务运行角度，本项目依赖以下外部产品：

- 火山 ASR：用于音频转写
- TOS：用于音频对象存储和预签名访问
- OpenAI 兼容 / Anthropic 兼容 LLM：用于纠错、Notes、Minutes、KBQ、CA
- Ollama：用于本地 embedding
- Qdrant：用于 summary 检索

## 4. 项目工作流

### 4.1 项目级工作流

1. 创建项目
2. 上传项目指南文件
3. BFF 后台触发项目指南学习
4. 提取文本/OCR 兜底
5. LLM 总结指南，回写项目背景与 guide 表
6. 创建项目角色、问卷、Key BQ

### 4.2 访谈级工作流

1. 在项目下创建访谈并上传音频
2. BFF 保存音频到本地 `audio/` 和 `data/` 备份目录
3. BFF 调用 Engine 内部接口触发转录工作流
4. Engine 上传音频到 TOS 或复用已有对象
5. Engine 调火山 ASR，支持异步轮询与断点恢复
6. Engine 对 ASR 结果清洗和纠错
7. 将清洗后的段落写入 `bh_project_interview_summary`
8. 基于 summary 建立 Qdrant 向量索引
9. 根据问卷拆出的题目生成 Notes
10. 基于 summary 全文生成 Minutes
11. 基于 Minutes 生成 Cards
12. 基于 Key BQ + Minutes 生成 KBQ Notes
13. 前端查看、编辑、导出 transcript / overall notes / cards

### 4.3 项目级 CA 工作流

1. 收集项目下访谈的 Minutes 文本
2. 按问卷维度构建对比框架
3. LLM 生成 CA framework
4. 按访谈维度补齐 cell 内容
5. 保存 `ca_json/framework_json/final_json`
6. 前端展示并支持导出 Word/XLSX

## 5. 工作流各步骤核心代码文件

| 步骤 | 核心文件 | 主要职责 |
| --- | --- | --- |
| 运行时配置 | [config.py](/config.py) | 统一读取 `.env`，提供全局运行时配置 |
| 统一启动 | [start_all.sh](/start_all.sh) | 启停 Engine/BFF/FE，管理 PID、端口和日志 |
| BFF 入口 | [summarynotes-be/main.py](/main.py) | 注册外部 API，启动时恢复项目指南任务 |
| Engine 入口 | [Api.py](/Api.py) | 暴露内部工作流 API，启动时恢复转录任务 |
| 项目管理 | [summarynotes-be/api/projects.py](/projects.py) | 项目 CRUD、指南上传、CA 查询与导出 |
| 问卷管理 | [summarynotes-be/api/project_questionnaires.py](/project_questionnaires.py) | 问卷上传、docx 解析、热词管理 |
| 访谈创建 | [summarynotes-be/api/project_interviews.py](/project_interviews.py) | 创建访谈、保存音频、触发转录工作流 |
| 访谈读写 | [summarynotes-be/api/interviews.py](/interviews.py) | transcript、notes、minutes、cards、导出、few-shot、编辑 |
| 项目指南学习 | [summarynotes-be/guide_workflow.py](/guide_workflow.py) | 指南文本抽取、OCR 兜底、项目背景总结 |
| 转录主工作流 | [Workflow.py](/Workflow.py) | 音频上传、ASR、清洗、summary 写库、minutes/cards/kbq 链式触发 |
| ASR 上传 | [VolcUpload.py](/VolcUpload.py) | 本地音频路径解析、TOS 上传、预签名 URL |
| ASR 调用 | [VolcengineConversion.py](/VolcengineConversion.py) | 调火山 ASR 接口 |
| 文本清洗纠错 | [CleanConversion.py](/CleanConversion.py) | 纠错、规范化 ASR 输出 |
| LLM 调用封装 | [Model.py](/Model.py)、[LLMProviders.py](/LLMProviders.py) | 统一封装不同模型供应商调用 |
| RAG 索引 | [RagIndex.py](/RagIndex.py) | 写入 Qdrant、按问题检索 summary 片段 |
| Questions/Notes | [NotesWorkflow.py](/NotesWorkflow.py) | 读取题目、RAG 检索、few-shot 选择、生成并写入 Notes |
| Minutes | [MinutesWorkflow.py](/MinutesWorkflow.py) | 基于 summary 全文生成访谈级智能纪要 |
| KBQ Notes | [KBQNotesWorkflow.py](/KBQNotesWorkflow.py) | 基于 key BQ + Minutes 生成 KBQ 内容 |
| 全文卡片 | [CardsWorkflow.py](/CardsWorkflow.py) | 基于 Minutes 生成模块总结 Cards |
| CA 表 | [CAWorkflow.py](/CAWorkflow.py) | 汇总多访谈 Minutes 生成项目 CA 表 |
| 数据访问 | [DbAccess.py](/DbAccess.py)、[summarynotes-be/db.py](/db.py) | Engine/BFF 两侧数据库读写 |
| 日志 | [InterviewLogger.py](/InterviewLogger.py) | 系统、项目、访谈维度日志落盘 |

## 6. 已完成功能

按代码现状，已经具备以下功能：

- 用户登录、Cookie 登录态维护
- 项目创建、编辑、删除、查询
- 项目指南上传与自动学习总结
- 项目角色管理能力基础设施
- 问卷上传、docx 转 md/json、热词审核
- 项目级 Key BQ 管理
- 项目下创建访谈并上传音频
- 访谈元信息维护与显示名生成
- 转录工作流后台异步执行
- 转录任务断点恢复与重启续跑
- summary 查询、逐条编辑、纠错学习记录
- 问题列表查看、手工新增/删除问题
- 基于问题生成 Notes
- overall notes 页面数据聚合
- Minutes 生成与手工保存
- KBQ Notes 刷新
- 全文 Cards 生成、增删改、审核字段保存
- Few-shot 样本管理
- Transcript / Overall Notes / CA 导出 Word、XLSX
- CA 表生成、框架保存、结果导出

## 7. 数据库字段说明

数据库初始化脚本见 [schema.sql]，数据库名为 `summarynotes`。

### 7.1 用户与基础字典

#### `bh_user`

- `id`：用户 ID
- `username`：登录用户名
- `password_hash`：当前登录使用的密码字段
- `created_at` `updated_at`：创建/更新时间

#### `bh_question_intent`

- `id`：意图 ID
- `code`：意图编码
- `name`：意图名称
- `description`：意图说明
- `schema_name`：结构名
- `status`：启用状态

- 这个表在当前链路中暂时没用
  
### 7.2 项目域

#### `bh_project`

- `id`：项目 ID
- `name`：项目名称
- `keywords`：项目关键词
- `core_problem`：项目核心背景
- `created_by_user_id`：项目创建人，一个用户只能看见自己创建的项目
- `key_bq_json`：项目级共享 Key BQ
- `guide_file_name` `guide_file_path`：指南文件信息

#### `bh_project_role`

- `id`：角色 ID
- `project_id`：所属项目
- `role_name`：角色名称
- `role_type`：`doctor/patient/custom`
- `detail_schema_json`：访谈详情字段模板

#### `bh_project_questionnaire`

- `id`：问卷 ID
- `project_id`：所属项目
- `role_id`：关联角色
- `name`：问卷名称
- `file_name`：原始文件名
- `docx_path` `md_path` `json_path`：解析后文件路径
- `hotwords`：热词列表 JSON
- `status`：`hotword_review_pending/ready/failed`
- `object_type`：`doctor/patient`

#### `bh_project_key_bq`

- `id`：项目级 Key BQ 记录
- `project_id`：所属项目
- `name`：Key BQ 组名称
- `key_bq_json`：Key BQ JSON 内容

#### `bh_project_guide`

- `id`：指南记录
- `project_id`：所属项目
- `guide_file_name` `guide_file_path`：原始/存储路径
- `file_type`：`pdf/docx/md/xlsx`
- `extracted_text`：抽取出的全文
- `summary_text`：模型生成的项目背景总结
- `status`：`queued/extracting/summarizing/done/failed`
- `error_message`：失败原因
- `guide_files_json`：多文件 manifest

#### `bh_project_ca_table`

- `id`：CA 主键
- `project_id`：所属项目
- `questionnaire_id`：可选问卷维度
- `ca_json`：当前活跃 CA
- `framework_json`：框架稿
- `final_json`：最终稿
- `framework_status`：`draft/reviewing/reviewed`
- `final_status`：`pending/generating/done/failed`

### 7.3 访谈域

#### `bh_project_interview`

- `id`：访谈 ID
- `parse_project_id`：所属项目
- `name`：访谈名称/显示名
- `keywords` `core_problem`：访谈级关键词/背景
- `questionnaire_id`：关联问卷
- `key_bq_id`：关联项目级 Key BQ
- `interview_date`：访谈日期
- `hospital_city` `hospital_decile` `doctor_level`：核心元信息
- `file_id` `file_path` `file_name`：音频文件信息
- `file_content`：原始转录文本兼容字段
- `note_content`：旧版 notes/纪要兼容字段
- `status`：访谈工作流状态
- `problem_answer`：结构化问答 JSON

#### `bh_interview_detail`

- `interview_id`：关联访谈
- `detail_json`：扩展元信息 JSON
- `doctor_level` `doctor_title` `city` `hospital` `department` `hospital_decile`：常用字段冗余列，便于查询/展示

#### `bh_interview_workflow_jobs`

- `project_id` `project_interview_id`：任务归属
- `workflow_type`：当前主要为 `transcription`
- `status`：`queued/running/done/failed` 等
- `stage`：当前阶段，如 `audio_ready/asr_polling/summary_written/done`
- `object_key` `audio_url`：音频对象信息
- `volc_task_id`：火山任务 ID
- `next_poll_at` `last_polled_at` `task_expires_at`：轮询控制
- `retry_count` `poll_count`：重试和轮询统计
- `lease_owner` `lease_expires_at`：恢复任务租约
- `checkpoint_json`：恢复快照
- `asr_result_json` `cleaned_json`：中间结果
- `error_stage` `error_message` `error_traceback`：错误信息

#### `bh_project_interview_summary`

- `id`：summary 段 ID
- `project_interview_id`：所属访谈
- `timestamp`：时间区间
- `speaker`：说话人
- `text`：清洗后的 transcript 片段
- `confidence`：置信度
- `modify`：是否被人工修改

#### `bh_transcription_corrections`

- `project_id` `project_interview_id` `summary_id`：纠错归属
- `wrong_text` `correct_text`：错误片段与修正文本
- `context_before` `context_after`：上下文
- `edit_type`：修正类型
- `confidence`：规则置信度
- `usage_count`：后续复用次数
- `status`：`pending/approved/rejected`

### 7.4 题目、Notes 与沉淀内容

#### `bh_project_question`

- `project_interview_id`：所属访谈
- `question_order`：题目顺序
- `question_text`：题干
- `question_type`：题型
- `research_phase`：研究阶段
- `intent_id`：问题意图
- `meta`：题目来源、附加属性 JSON

- 前期有使用，后期被废弃了，保留即可
  
#### `bh_project_interview_notes`

- `project_id` `project_interview_id` `question_id` `intent_id`：归属关系
- `note_json`：Notes 主体 JSON
- `confidence`：生成置信度
- `status`：生成状态
- `error_message`：异常说明
- `created_by` `last_updated_by`：编辑人信息

- 被废弃，保留即可

#### `bh_project_notes_edit_log`

- `notes_result_id`：对应 notes
- `editor_id`：编辑人
- `action`：动作类型
- `before_json` `after_json`：编辑前后内容
- `quality_score`：质量分
- `comment`：备注

- 未使用，保留即可
  
#### `bh_project_fewshot_sample`

- `project_id` `project_interview_id`：样本归属
- `question_id` `intent_id`：问题归属
- `notes_result_id`：来源 notes
- `sample_json`：few-shot 样本
- `quality_score`：质量评分
- `source_kind`：样本来源

- 被废弃，后期需要做自学习的时候可以使用，保留即可
  
#### `bh_project_interview_minutes`

- `project_id` `project_interview_id`：归属
- `outline_json`：纪要大纲
- `minutes_json`：最终纪要 JSON
- `status`：`pending/done/failed`
- `error_message`：异常信息

#### `bh_project_interview_key_bq`

- `project_id` `project_interview_id`：归属
- `bq_order`：Key BQ 顺序
- `bq_text`：Key BQ 原文
- `dimension_json`：维度抽取结果
- `note_json`：KBQ Notes 结果
- `status`：`pending/done/failed`

#### `bh_project_interview_cards`

- `project_id` `project_interview_id`：归属
- `status`：Cards 整体状态
- `error_message`：整体失败信息

#### `bh_project_interview_cards_items`

- `cards_id`：所属卡片集合
- `card_order`：卡片顺序
- `card_title`：标题
- `card_summary`：摘要
- `generated_json`：模型原始结果
- `final_json`：前端/人工修订后的最终结果
- `review_status`：`pending/approved/rejected/needs_revision`
- `review_comment`：审核备注

## 8. API 接口说明

### 8.1 外部 BFF 接口

服务入口：`summarynotes-be/main.py`

#### 认证

- `POST /api/auth/login`：登录
- `GET /api/auth/me`：获取当前登录用户
- `POST /api/auth/logout`：退出登录

- 不做注册，账号在后台数据库直接开
  
#### 项目

- `POST /api/projects`：创建项目
- `PUT /api/projects/{project_id}`：更新项目
- `GET /api/projects`：项目列表
- `GET /api/projects/{project_id}`：项目详情
- `DELETE /api/projects/{project_id}`：删除项目
- `POST /api/projects/{project_id}/guide`：上传项目指南并后台学习

#### 问卷

- `POST /api/projects/{project_id}/questionnaires`：上传问卷
- `GET /api/projects/{project_id}/questionnaires`：问卷列表
- `GET /api/projects/{project_id}/questionnaires/{questionnaire_id}`：问卷详情
- `PUT /api/projects/{project_id}/questionnaires/{questionnaire_id}/hotwords`：保存问卷热词
- `DELETE /api/projects/{project_id}/questionnaires/{questionnaire_id}`：删除问卷

#### 项目 Key BQ

- `POST /api/projects/{project_id}/key-bq`：新增 Key BQ
- `GET /api/projects/{project_id}/key-bq/current`：读取当前 Key BQ
- `PUT /api/projects/{project_id}/key-bq/current`：更新当前 Key BQ
- `GET /api/projects/{project_id}/key-bq`：Key BQ 列表
- `GET /api/projects/{project_id}/key-bq/{key_bq_id}`：Key BQ 详情
- `PUT /api/projects/{project_id}/key-bq/{key_bq_id}`：更新指定 Key BQ
- `DELETE /api/projects/{project_id}/key-bq/{key_bq_id}`：删除指定 Key BQ

#### 访谈创建

- `POST /api/projects/{project_id}/interviews`：创建访谈并上传音频
- `GET /api/projects/{project_id}/interviews`：项目下访谈列表
- `GET /api/projects/{project_id}/interviews/{interview_id}/questionnaire-hotwords`：读取待确认热词
- `POST /api/projects/{project_id}/interviews/{interview_id}/questionnaire-hotwords`：确认热词

#### 访谈明细

- `POST /api/interviews/{interview_id}/run`：手动重跑工作流
- `GET /api/interviews/{interview_id}/status`：获取访谈状态
- `GET /api/interviews/{interview_id}`：访谈详情
- `PATCH /api/interviews/{interview_id}/detail`：更新访谈细节字段
- `PATCH /api/interviews/{interview_id}/name`：修改访谈显示名
- `GET /api/interviews/{interview_id}/audio`：获取音频
- `DELETE /api/interviews/{interview_id}`：删除访谈

#### Questions / Notes / Summary

- `GET /api/interviews/{interview_id}/questions`：问题列表
- `POST /api/interviews/{interview_id}/questions`：新增问题
- `DELETE /api/interviews/{interview_id}/questions/{question_id}`：删除问题
- `POST /api/interviews/{interview_id}/questions/{question_id}/generate-notes`：生成单题 Notes
- `GET /api/interviews/{interview_id}/notes`：获取 Notes
- `GET /api/interviews/{interview_id}/summary`：获取 transcript summary
- `PATCH /api/interviews/{interview_id}/summary/{summary_id}`：编辑单条 summary
- `PATCH /api/interviews/{interview_id}/summary/{summary_id}/complete`：标记 summary 编辑完成

#### Overall Notes / Minutes / KBQ

- `GET /api/interviews/{interview_id}/overall-notes`：获取 overall notes 聚合结果
- `PUT /api/interviews/{interview_id}/overall-notes/summary`：保存 overall summary
- `PUT /api/interviews/{interview_id}/overall-notes/kbq/{kbq_id}`：保存单条 KBQ 内容
- `PUT /api/interviews/{interview_id}/overall-notes/minutes`：保存 minutes
- `POST /api/interviews/{interview_id}/kbq-notes/refresh`：刷新 KBQ Notes
- `POST /api/interviews/{interview_id}/minutes/refresh`：刷新 Minutes

#### Cards

- `GET /api/interviews/{interview_id}/cards`：获取 Cards
- `POST /api/interviews/{interview_id}/cards/refresh`：重新生成 Cards
- `POST /api/interviews/{interview_id}/cards/items`：新增卡片项
- `PUT /api/interviews/{interview_id}/cards/items/{item_id}`：编辑卡片项
- `DELETE /api/interviews/{interview_id}/cards/items/{item_id}`：删除卡片项

#### Few-shot

- `GET /api/interviews/{interview_id}/fewshot-samples`：样本列表
- `POST /api/interviews/{interview_id}/questions/{question_id}/fewshot-samples`：新增样本
- `DELETE /api/interviews/{interview_id}/fewshot-samples/{sample_id}`：删除样本

#### CA / 导出

- `GET /api/projects/{project_id}/ca-table`：获取当前 CA
- `GET /api/projects/{project_id}/ca-tables`：获取 CA 列表
- `POST /api/projects/{project_id}/ca-table/generate`：生成 CA
- `POST /api/projects/{project_id}/ca-table/framework`：保存 CA 框架
- `POST /api/projects/{project_id}/ca-table/export-xlsx`：导出 CA Excel
- `POST /api/projects/{project_id}/ca-table/export-word`：导出 CA Word
- `GET /api/interviews/{interview_id}/trans/export-word`：导出 transcript Word
- `GET /api/interviews/{interview_id}/overall-notes/export-word`：预览 overall notes 导出内容
- `POST /api/interviews/{interview_id}/overall-notes/export-word`：导出 overall notes Word

#### 其他

- `GET /api/interview-detail-fields`：获取访谈详情字段定义
- `GET /api/question-intents`：获取问题意图字典

### 8.2 内部 Engine 接口

服务入口：`Api.py`

- `POST /internal/interviews/{interview_id}/transcribe`：提交完整转录工作流
- `POST /internal/interviews/{interview_id}/run-workflow`：同上，兼容别名
- `POST /internal/interviews/{interview_id}/generate-notes`：生成该访谈 Notes
- `POST /internal/interviews/{interview_id}/generate-minutes`：生成 Minutes
- `POST /internal/interviews/{interview_id}/refresh-kbq-notes`：刷新 KBQ Notes
- `POST /internal/interviews/{interview_id}/reindex-rag`：重建该访谈 RAG 索引
- `GET /internal/interviews/{interview_id}/notes`：读取 Notes
- `GET /internal/interviews/{interview_id}/questions`：读取问题列表
- `POST /internal/projects/{project_id}/generate-ca-table`：生成项目 CA 表

## 9. 日志系统

日志工具在 [InterviewLogger.py](/InterviewLogger.py)。

### 9.1 日志目录

- 系统日志：`runtime/logs/system.log`
- 访谈日志：`runtime/logs/interviews/interview_{id}.log`
- 项目日志：`runtime/logs/projects/project_{id}.log`
- 服务启动日志：`runtime/logs/engine.log` `runtime/logs/bff.log` `runtime/logs/fe.log`

### 9.2 日志写法

- `log_interview(component, interview_id, message)`：写访谈级日志
- `log_project(component, project_id, message)`：写项目级日志

日志格式统一为：

```text
YYYY-MM-DD HH:MM:SS [COMPONENT] interview_id=123 message
YYYY-MM-DD HH:MM:SS [COMPONENT] project_id=12 message
```

### 9.3 常见组件前缀

- `TRANSCRIBE`：转录任务提交与恢复
- `WORKFLOW`：主工作流分阶段日志
- `NOTES`：题目 Notes 生成
- `MINUTES`：智能纪要生成
- `KBQ`：KBQ Notes 生成
- `CA`：项目级 CA 生成
- `GUIDE`：项目指南学习

## 10. 交接建议与注意事项

1. 优先看 `start_all.sh -> BFF API -> Engine Workflow -> DbAccess/summarynotes-be/db.py` 这条主线。
2. 当前数据库访问分成两套：Engine 用 `DbAccess.py`，BFF 用 `summarynotes-be/db.py`，后续维护要注意双边逻辑是否同步。
3. 工作流恢复依赖 `bh_interview_workflow_jobs`，排查“任务卡住”时先看此表，再看对应 `runtime/logs/interviews/interview_{id}.log`。
4. 项目指南、问卷、minutes、cards、导出文件都会落到本地 `data/`，音频会落到 `audio/` 和 TOS。
5. 前端默认访问 `9000`，如果单独部署，需要确认 `NEXT_PUBLIC_API_BASE_URL`。
6. `summarynotes-be/api/health.py` 虽然定义了 `/api/health`，但当前 `summarynotes-be/main.py` 没有注册该 router；如果需要健康检查，要补 `include_router`。
7. 认证接口当前是把登录输入直接与 `bh_user.password_hash` 字段做字符串比较，不是标准哈希校验；如果后续上生产，要先补密码加密与校验逻辑。
8. 根 README 仍有旧描述，实际交接请以本文件、`environment.yml`、`schema.sql` 和现有代码为准。
