# Work Buddy 项目部署文档

## 1. 文档目的

本文档用于在一台新的机器上完整部署 Work Buddy 项目，包括：

1. 数据库迁移
2. 在 Docker 中启动 Qdrant
3. 本地安装 Ollama 并拉取指定模型
4. 前端环境准备与编译
5. Python/Conda 虚拟环境配置，保持与项目一致
6. 整体服务拉起

本文档以当前仓库代码为准，不以旧版 README 为准。

## 2. 部署目标与端口

项目由三部分组成：

- Engine：根目录 FastAPI 服务，默认端口 `8000`
- BFF：`summarynotes-be/` 下 FastAPI 服务，默认端口 `9000`
- FE：`summarynotes-fe/` 下 Next.js 服务，默认端口 `3000`

- 注意端口不要被占用，或者换端口部署

统一启动脚本：

- [start_all.sh](/start_all.sh)

## 3. 部署前准备

- Git
- Docker
- Conda
- Node.js + npm
- MySQL 8.x
- 可以访问外部 LLM / ASR / TOS 服务

本项目还依赖以下外部服务或账号：

- MySQL
- Qdrant
- Ollama
- OpenAI 兼容或 Anthropic 兼容模型服务
- 火山 ASR
- 字节 TOS

## 4. 代码获取

```bash
git clone url
cd NotesSummary
```

## 5. 数据库迁移

### 5.1 数据库说明

数据库初始化脚本在：

- [schema.sql](/schema.sql)


### 5.2 创建数据库并导入结构

先登录 MySQL：

```bash
mysql -uroot -p
```

或者直接导入：

```bash
mysql -uroot -p < schema.sql
```

如果只想指定目标库导入，也可以先手工建库：

```sql
CREATE DATABASE summarynotes DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
```

然后执行：

```bash
mysql -uroot -p summarynotes < schema.sql
```

### 5.3 数据库配置项

根目录 `.env` 中至少需要配置：

```env
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=summarynotes
```

## 6. 在 Docker 里启动 Qdrant

### 6.1 启动命令

项目默认使用：

- `QDRANT_HOST=127.0.0.1`
- `QDRANT_PORT=6333`
- `QDRANT_COLLECTION_SUMMARY=interview_summary`

使用下面的 Docker 命令拉起 Qdrant：

```bash
docker run -d \
  --name qdrant \
  -p 6333:6333 \
  -p 6334:6334 \
  -v $(pwd)/runtime/qdrant_storage:/qdrant/storage \
  qdrant/qdrant
```

说明：

- `6333`：HTTP API 端口
- `6334`：gRPC 端口
- `runtime/qdrant_storage`：本地持久化目录

### 6.2 验证 Qdrant

```bash
curl http://127.0.0.1:6333/collections
```

返回 JSON 即表示服务可用。

### 6.3 对应 `.env` 配置

```env
QDRANT_HOST=127.0.0.1
QDRANT_PORT=6333
QDRANT_COLLECTION_SUMMARY=interview_summary
```

## 7. 本地部署 Ollama 并拉取模型

### 7.1 安装 Ollama

Ollama 需要本地安装，不放在本项目 Docker 里。

Linux 常见安装方式：

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

安装后启动服务：

```bash
ollama serve
```


### 7.2 拉取项目所需模型

当前项目配置默认模型名来自 [config.py](/config.py)：

- `OLLAMA_MODEL_NAME=bge-m3:latest`

执行：

```bash
ollama pull bge-m3:latest
```

查看本地模型：

```bash
ollama list
```

### 7.3 对应 `.env` 配置

```env
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL_NAME=bge-m3:latest
```

### 7.4 验证 Ollama

```bash
curl http://127.0.0.1:11434/api/tags
```

能看到 `bge-m3:latest` 即表示配置正常。

## 8. 前端环境、nvm 版本与编译

### 8.1 Node.js 版本建议

前端使用：

- Next.js `16.2.3`
- React `19.2.4`

示例：

```bash
nvm install 20
nvm use 20
node -v
npm -v
```

### 8.2 安装前端依赖

前端目录：

- [summarynotes-fe/package.json](/package.json)

安装命令：

```bash
cd summarynotes-fe
npm install
```

如果需要严格按锁文件安装：

```bash
npm ci
```

说明：

- 仓库内已有 `package-lock.json`
- 生产部署建议优先 `npm ci`

### 8.3 前端编译

```bash
cd summarynotes-fe
npm run build
```

本地开发启动：

```bash
npm run dev
```

生产启动：

```bash
npm run start -- --hostname 0.0.0.0 --port 3000
```

### 8.4 前端访问后端地址

统一启动脚本会自动生成：

```env
NEXT_PUBLIC_API_BASE_URL=http://<机器IP>:9000
```

如果手工部署，建议显式设置：

```bash
export NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:9000
```

如果前端端口后续修改，记得同步检查 BFF 的 CORS 白名单文件：

- [summarynotes-be/middleware/cors.py](/cors.py)

## 9. 虚拟环境配置

### 9.1 Conda 环境名称

项目当前环境名固定为：

- `vol`

定义文件：

- [environment.yml](/environment.yml)

### 9.2 创建环境

```bash
conda env create -f environment.yml
conda activate vol
```

### 9.3 已有环境同步依赖

```bash
conda env update -n vol -f environment.yml --prune
```

### 9.4 保持环境名称和依赖一致

请保持以下一致性：

- 环境名称：`vol`
- Python 版本：`3.13`
- `environment.yml` 中 pip 依赖版本不随意改动

如果后续团队改成 `requirements.txt` 方式，应先统一方案后再变更；当前仓库仍以 `environment.yml` 为基准。

## 10. 根目录 `.env` 配置

项目根目录创建 `.env` 文件，建议最少包含如下内容：

```env
# 数据库
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=summarynotes

# 通用大模型
LLM_PROVIDER=openai
LLM_API_KEY=your_llm_api_key
LLM_BASE_URL=https://your-llm-endpoint
LLM_MODEL_NAME=your_model_name

# Transcript / 转录纠错模型
TRANSCRIPT_LLM_PROVIDER=openai
TRANSCRIPT_LLM_API_KEY=your_transcript_llm_api_key
TRANSCRIPT_LLM_BASE_URL=https://your-transcript-llm-endpoint
TRANSCRIPT_LLM_MODEL_NAME=your_transcript_model

# Notes / Minutes / KBQ / CA 模型
NOTES_LLM_PROVIDER=openai
NOTES_LLM_API_KEY=your_notes_llm_api_key
NOTES_LLM_BASE_URL=https://your-notes-llm-endpoint
NOTES_LLM_MODEL_NAME=your_notes_model

# Ollama
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL_NAME=bge-m3:latest

# 热词兜底文件
TERM_HINTS_FILE=data/keywords.txt

# ASR
ASR_APP_KEY=your_asr_app_key
ASR_ACCESS_KEY=your_asr_access_key
VOLCANO_CLUSTER=volc_auc_common
VOLCANO_SERVICE_URL=https://openspeech.bytedance.com/api/v1/auc

# TOS
TOS_ACCESS_KEY=your_tos_access_key
TOS_SECRET_KEY=your_tos_secret_key
TOS_ENDPOINT=https://tos-cn-shanghai.volces.com
TOS_REGION=cn-shanghai
TOS_BUCKET_NAME=benhealth
LOCAL_AUDIO_ROOT=.
TOS_AUDIO_PREFIX=audio
TOS_URL_EXPIRE_SECONDS=3600

# Qdrant
QDRANT_HOST=127.0.0.1
QDRANT_PORT=6333
QDRANT_COLLECTION_SUMMARY=interview_summary

# 内部服务
INTERNAL_SERVICE_BASE=http://127.0.0.1:8000

# 可选：统一脚本使用的 conda 环境名
APP_CONDA_ENV=vol
```

说明：

- `APP_CONDA_ENV=vol` 能让 [start_all.sh](/start_all.sh) 自动用 conda 环境启动 Engine 和 BFF
- `.env` 改动后要重启相关服务

## 11. 整体服务拉起

### 11.1 推荐启动顺序

1. MySQL
2. Qdrant
3. Ollama
4. Conda 环境
5. 前端依赖安装
6. 整体脚本启动

### 11.2 一键启动

在项目根目录执行：

```bash
conda activate vol
bash start_all.sh start
```

### 11.3 查看状态

```bash
bash start_all.sh status
```

### 11.4 停止服务

```bash
bash start_all.sh stop
```

### 11.5 重启服务

```bash
bash start_all.sh restart
```

## 12. 服务验证

### 12.1 Engine

```bash
curl http://127.0.0.1:8000/docs
```

### 12.2 BFF

```bash
curl http://127.0.0.1:9000/docs
```

### 12.3 前端

浏览器打开：

```text
http://127.0.0.1:3000
```

### 12.4 Qdrant

```bash
curl http://127.0.0.1:6333/collections
```

### 12.5 Ollama

```bash
curl http://127.0.0.1:11434/api/tags
```

## 13. 常见问题

### 13.1 数据库连不上

检查：

- `.env` 中 `DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME`
- MySQL 是否允许本机连接

### 13.2 Qdrant 正常但检索失败

检查：

- Qdrant 容器是否启动
- `QDRANT_HOST` 和 `QDRANT_PORT` 是否匹配
- Ollama embedding 模型是否已拉取

### 13.3 Ollama 服务未启动

检查：

```bash
ollama list
curl http://127.0.0.1:11434/api/tags
```

如果失败，重新执行：

```bash
ollama serve
```

### 13.4 前端构建失败

优先检查：

- `node -v`
- `npm -v`
- 是否使用了过老 Node 版本

建议：

```bash
nvm use 20
```

### 13.5 脚本启动失败

查看日志目录：

- `runtime/logs/engine.log`
- `runtime/logs/bff.log`
- `runtime/logs/fe.log`
- `runtime/logs/interviews/`
- `runtime/logs/projects/`

## 14. 部署完成后的最小检查清单

- MySQL 已导入 `schema.sql`
- `.env` 中 `DB_NAME=summarynotes`
- Qdrant Docker 容器已启动
- Ollama 已启动且已拉取 `bge-m3:latest`
- `conda activate vol` 正常
- `npm ci` 或 `npm install` 完成
- `bash start_all.sh start` 后 8000 / 9000 / 3000 端口均可访问
